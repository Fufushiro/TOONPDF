# Example keystore.properties for signing the release APK
# Copy this file to keystore/keystore.properties and fill in your values.
# Paths can be absolute or relative to the project root.

# Relative path to your JKS/Keystore file
storeFile=keystore/KEYSTORE.jks

# Store password for the keystore
storePassword=CHANGE_ME

# Alias of the key within the keystore
keyAlias=CHANGE_ME

# Password for the key alias
keyPassword=CHANGE_ME

